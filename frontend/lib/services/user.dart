import 'package:iiitb_hogwarts/models/user.dart';

Future<bool> userLoginStatus(User user) async {
  // TODO: Implement this function
  // 1. It should check if token is stored in sharedPreferences
  // 2. If token is not present return false, otherwise initialize current user
  return true;
}